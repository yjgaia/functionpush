export default class Server {

    constructor(
        port: number, keyPath: string, certPath: string,
        httpPort: number,
    ) {

        //TODO: web server
        //TODO: web socket server
    }
}
