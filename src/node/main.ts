const functionpush = (options: {
    httpPort?: number,
    httpsPort?: number,
    sslKeyFilePath?: number,
    sslCertFilePath?: number,
}) => {
    console.log("functionpush");
};

export = functionpush;
